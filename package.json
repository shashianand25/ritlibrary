{
  "name": "ritlibrary-monorepo",
  "version": "1.1.0",
  "private": true,
  "description": "RIT Library - Academic resource portal with notes, PYQs, and syllabus tracking",
  "workspaces": [
    "my-project",
    "library-backend"
  ],
  "scripts": {
    "dev": "npm --prefix my-project run dev",
    "build": "npm --prefix my-project run build",
    "test": "npm --prefix my-project test && npm --prefix library-backend test",
    "test:coverage": "npm --prefix my-project run test:coverage",
    "typecheck": "npm --prefix my-project run typecheck",
    "lint": "npm --prefix my-project run lint && npm --prefix library-backend run lint",
    "format": "npm --prefix my-project run format && npm --prefix library-backend run format",
    "format:check": "npm --prefix my-project run format:check && npm --prefix library-backend run format:check"
  },
  "devDependencies": {
    "prettier": "^3.9.6"
  },
  "author": "RIT Library Team",
  "license": "ISC",
  "repository": {
    "type": "git",
    "url": "https://github.com/shashianand25/ritlibrary"
  }
}
